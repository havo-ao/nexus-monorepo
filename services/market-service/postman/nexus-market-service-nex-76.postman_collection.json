{
  "info": {
    "name": "Nexus Market Service - NEX-76",
    "description": "Manual validation collection for NEX-76: consult instrument detail.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "variable": [
    {
      "key": "baseUrl",
      "value": "http://localhost:8884"
    }
  ],
  "item": [
    {
      "name": "Synchronize AAPL quote",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "body": {
          "mode": "raw",
          "raw": "{\n  \"symbols\": [\"AAPL\"],\n  \"requestedBy\": \"system@nexus.local\"\n}"
        },
        "url": {
          "raw": "{{baseUrl}}/api/v1/quotes/sync",
          "host": ["{{baseUrl}}"],
          "path": ["api", "v1", "quotes", "sync"]
        },
        "description": "Seeds latest quote data used by instrument detail."
      }
    },
    {
      "name": "Get AAPL detail",
      "request": {
        "method": "GET",
        "url": {
          "raw": "{{baseUrl}}/api/v1/instruments/AAPL",
          "host": ["{{baseUrl}}"],
          "path": ["api", "v1", "instruments", "AAPL"]
        },
        "description": "Returns instrument metadata and latest available quote when present."
      }
    }
  ]
}
